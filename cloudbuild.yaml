steps:
  # 1. Docker イメージをビルド
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/winwinreco:$COMMIT_SHA'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/winwinreco:latest'
      - '.'

  # 2. イメージを Artifact Registry へプッシュ
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - push
      - '--all-tags'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/winwinreco'

  # 3. Cloud Run へデプロイ
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - winwinreco
      - '--image=asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/winwinreco:$COMMIT_SHA'
      - '--region=asia-northeast1'
      - '--platform=managed'
      - '--quiet'
      - '--min-instances=0'
      - '--max-instances=2'
      - '--concurrency=80'
      - '--memory=512Mi'
      - '--set-env-vars=DB_DRIVER=firestore,GOOGLE_CLOUD_PROJECT=$PROJECT_ID,AI_PROVIDER=groq,STT_PROVIDER=elevenlabs,STT_LANGUAGE=ja,ELEVENLABS_STT_MODEL=scribe_v2,ELEVENLABS_STT_REALTIME_MODEL=scribe_v2_realtime,COOKIE_SECURE=true,WS_ALLOWED_ORIGINS=https://winwinreco-227823039350.asia-northeast1.run.app,OWNER_EMAIL=ninnzinn.3bonn@gmail.com'
      - '--set-secrets=GROQ_API_KEY=groq-api-key:latest,GEMINI_API_KEY=gemini-api-key:latest,ELEVENLABS_API_KEY=elevenlabs-api-key:latest'

images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/winwinreco:$COMMIT_SHA'
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/winwinreco:latest'

options:
  logging: CLOUD_LOGGING_ONLY
