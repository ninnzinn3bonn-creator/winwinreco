# プロジェクト進捗・実装計画 (Meeting Minutes App)

## 1. これまでの作業実績 (2026-03-05)
- **MVP機能の完成**: 
    - Google Cloud Speech-to-Text を利用したリアルタイム文字起こし。
    - WebSocket による複数デバイス間の発言同期。
    - SQLite3 による発言ログの永続化。
    - 会議終了後の Markdown 形式での議事録ダウンロード。
- **AI分析機能の初期実装**:
    - Gemini 1.5 Flash を用いた「要約」「決定事項/TODO」の生成機能を実装。
    - 会議ログをコンテキストとして渡し、精度の高い分析を実現。
- **プロジェクト構成の整理**:
    - `README.md` の更新と不要な一時ファイルの削除。

## 2. 今後の実装計画: ローカルLLM (Ollama) 統合 [完了]
機密保持やオフライン対応のため、外部API（Gemini）に依存せず、ローカルマシンのLLMで分析を行えるようにしました。

### 実装済みタスク
1.  **AIプロバイダーの抽象化**: 
    - `AIService` をベースクラス化し、`GeminiProvider` と `OllamaProvider` を実装。
2.  **Ollama連携の実装**: 
    - `http://localhost:11434/api/generate` へのリクエスト処理を実装。
3.  **設定機能の追加**: 
    - `.env` で `AI_PROVIDER` (`gemini` or `ollama`) を切り替え可能。
- **UIの改善**:
    - 解析結果に使用されたAIエンジン名を表示するバッジを追加。
    - 改行表示の修正。

## 3. リアルタイム・トピック構造化機能 (2026-03-14) [実装完了]
会議中の対話をリアルタイムで解析し、階層的なトピックツリーを生成する機能を実装しました。

### 実装内容
1.  **トピックツリー生成ロジック**:
    - `AIService` に `topic_tree` プロンプトを追加。
    - 対話ログから「概念抽出」を行い、親子関係のあるツリー形式で出力。
2.  **UIの刷新 (3段構成)**:
    - **上段**: トピックツリー・ビュー。議論の構造を常時確認可能。
    - **中段**: リアルタイム発言ログ。
    - **下段**: 操作パネル（解析、メモ、保存ボタン）。
3.  **インタラクション**:
    - 「解析」ボタンを押すことで、その時点までの全ログを元にツリーを更新。

## 4. ユーザー側の準備手順 (ローカルLLM利用時)

ローカルLLMモードを使用する場合、ユーザーは以下の準備を行う必要があります。

1.  **Ollamaのインストール**: [ollama.com](https://ollama.com/) からインストール。
2.  **モデルのダウンロード**: ターミナルでモデルを取得。
    ```bash
    ollama pull llama3 # または日本語対応モデル (elyza:jp など)
    ```
3.  **Ollamaの起動**: `ollama serve` が実行されていることを確認。
4.  **アプリ設定**: `.env` ファイルを編集。
    ```env
    AI_PROVIDER=ollama
    OLLAMA_MODEL=llama3
    ```

## 4. 将来的な展望
- 録音データの音声ファイル保存機能。
- 複数ルームの管理機能（ダッシュボード）。
- UI/UXのモダン化（Tailwind CSSの導入検討）。
